apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata: 
  name: marketplace-service-ingress
  namespace: s65-2
spec:
  rules:
    - http:
        paths:
          - backend:
              serviceName: marketplace-service
              servicePort: 80